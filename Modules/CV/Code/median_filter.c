#include "Headers/median_filter.h"

gdImagePtr doMedianFilter(gdImagePtr im)
{
	//int i, j, kx, ky, sx = im.gdImageSX(), sy = im.gdImageSY();

	/*for(i = 1; i < sx - 1; i++)
		for(j = 1; j < sy - 1; j++)
		{
			for()
		}*/
	//return im;
	return 0;
}